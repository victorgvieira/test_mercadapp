<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">


    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.victorgomes.test.myapplication.view.productdetail.ProductDetailViewModel" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:background="@android:color/white"
            android:orientation="vertical"
            android:padding="16dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <androidx.appcompat.widget.AppCompatImageView
                    android:layout_width="240dp"
                    android:layout_height="0dp"
                    android:layout_gravity="center"
                    android:scaleType="centerCrop"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintDimensionRatio="3:4"
                    app:layout_constraintLeft_toLeftOf="parent"
                    app:layout_constraintRight_toRightOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:load_image="@{viewModel.productDetail.image}"
                    tools:src="@mipmap/ic_launcher_round" />

                <androidx.appcompat.widget.AppCompatImageView
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_tag_sale"
                    android:visibility="@{viewModel.productDetail.discountApplied ? View.VISIBLE : View.GONE}"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />
            </androidx.constraintlayout.widget.ConstraintLayout>


            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:maxLines="2"
                android:textAllCaps="false"
                android:textColor="@android:color/black"
                android:textSize="14sp"
                app:defaultOrNoDescription="@{viewModel.productDetail.name}"
                tools:text="Roupa de seda Roupa de seda Roupa de seda Roupa de seda " />

            <com.google.android.flexbox.FlexboxLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:visibility="@{viewModel.productDetail.showSizes ? View.VISIBLE : View.GONE}"
                app:addSizes="@{viewModel.productDetail.sizes}"
                app:alignContent="center"
                app:alignItems="baseline"
                app:flexDirection="row"
                app:flexWrap="wrap"
                app:justifyContent="flex_start">

            </com.google.android.flexbox.FlexboxLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:visibility="@{viewModel.productDetail.discountApplied ? View.VISIBLE : View.GONE}"
                app:defaultOrNoDescription="@{viewModel.productDetail.regularPrice}"
                app:striked="@{true}"
                tools:text="Roupa de seda Roupa de seda Roupa de seda Roupa de seda " />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@android:color/black"
                android:textSize="24sp"
                android:textStyle="bold"
                app:defaultOrNoDescription="@{viewModel.productDetail.actualPrice}"
                tools:text="Roupa de seda Roupa de seda Roupa de seda Roupa de seda " />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:visibility="@{viewModel.productDetail.discountApplied ? View.VISIBLE : View.GONE}"
                app:defaultOrNoDescription="@{viewModel.productDetail.discountText}"
                tools:text="Roupa de seda Roupa de seda Roupa de seda Roupa de seda " />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@android:color/black"
                android:textSize="20sp"
                app:defaultOrNoDescription="@{viewModel.productDetail.installments}"
                tools:text="Roupa de seda Roupa de seda Roupa de seda Roupa de seda " />
        </LinearLayout>
    </ScrollView>

</layout>